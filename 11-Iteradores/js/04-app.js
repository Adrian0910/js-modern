// While loop

let i = 1; //Inicializar el while

// Par y non con while

while(i <= 10){ //condicion

    if(i % 2 === 0){
        console.log(`El númeron ${i} es par`);
    }else{
        console.log(`El número ${i} es non`);
    }
    i++; //Incremento
}

